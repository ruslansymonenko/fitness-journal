generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Entry {
  id          String   @id @default(uuid())
  date        DateTime
  workoutType String
  duration    Int
  notes       String?

  @@map("entries")
}


